# Domain for TLS certificate
# For production, set this to your actual domain (e.g., kawa.wantsmore.coffee)
# For local development, leave as localhost
DOMAIN=localhost

# HTTPS port (default: 8443 for development, use 443 for production)
HTTPS_PORT=8443

# HTTP port
HTTP_PORT=8080

# Email for Let's Encrypt certificate notifications (required for production)
ACME_EMAIL=
