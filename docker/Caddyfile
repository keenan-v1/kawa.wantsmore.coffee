# Global options
{
    # Email for Let's Encrypt certificate notifications
    email {$ACME_EMAIL:}
}

{$DOMAIN:localhost} {
    reverse_proxy app:5173

    # TLS is automatic with Caddy when using a real domain
    # Let's Encrypt will be used automatically for non-localhost domains
}
